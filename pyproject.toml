[project]
name = "sensor-data-pipeline"
version = "0.1.0"
description = "Sensor data processing pipeline"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["sensor_pipeline"]

[tool.setuptools.package-dir]
sensor_pipeline = "src/sensor_pipeline"
